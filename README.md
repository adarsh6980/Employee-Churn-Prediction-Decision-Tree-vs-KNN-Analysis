# Employee-Churn-Prediction-Decision-Tree-vs-KNN-Analysis
Employee churn prediction uses Decision Tree—which splits data into clear rules based on features like salary—and KNN, which compares employees to their nearest peers. Decision Trees are highly interpretable, KNN is simple but less transparent. Both help companies target retention strategies effectively.
